database_host: "{{ vault_database_host }}"
database_port: "{{ vault_database_port }}"
database_name: "{{ vault_database_name }}"
database_user: "{{ vault_database_user }}"
database_password: "{{ vault_database_password }}"
mailer_transport: "{{ vault_mailer_transport }}"
mailer_host: "{{ vault_mailer_host }}"
mailer_user: "{{ vault_mailer_user }}"
mailer_password: "{{ vault_mailer_password }}"
mailer_encryption: "{{ vault_mailer_encryption }}"
mailer_port: "{{ vault_mailer_port }}"
secret: "{{ vault_secret }}"
enable_aui: "{{ vault_enable_aui }}"
url_wsdl: "{{ vault_url_wsdl }}"
paws_user: "{{ vault_paws_user }}"
paws_password: "{{ vault_paws_password }}"
app_user: "{{ vault_app_user }}"
app_password: "{{ vault_app_password }}"
intranet_access: "{{ vault_intranet_access }}"
attributs: "{{ vault_attributs }}"
no_reply_mail: "{{ vault_no_reply_mail }}"
front_host: "{{ vault_front_host }}"
session_max_idle_time: "{{ vault_session_max_idle_time }}"
session_name: "{{ vault_session_name }}"
session_remember_me_name: "{{ vault_session_remember_me_name }}"
version: "{{ vault_version }}"
ldap_host: "{{ vault_ldap_host }}"
ldap_port: "{{ vault_ldap_port }}"
ldap_version: "{{ vault_ldap_version }}"
ldap_SSL: "{{ vault_ldap_SSL }}"
ldap_TLS: "{{ vault_ldap_TLS }}"
base_dn: "{{ vault_base_dn }}"
filter: "{{ vault_filter }}"
domain: "{{ vault_domain }}"